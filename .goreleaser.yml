env:
  - TAG_VERSION={{ split .Version "-" }}
  # - TYPE_INTEGRSTION={{ index (split .Version "-") 1 }}
before:
  hooks:
builds:
  -
    id: controlTower
    env:
      - CGO_ENABLED=0
    mod_timestamp: '{{ .CommitTimestamp }}'
    flags:
      - -trimpath
    ldflags:
      - '-s -w -X main.version={{.Version}} -X main.commit={{.Commit}}'
    goos:
      - linux
    goarch:
      - amd64
    binary: 'main'
archives:
  - 
    id: s3HookArchive
    builds: [s3Hook]
    format: zip
    name_template: 's3-hook_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
  - 
    id: controlTowerArchive
    builds: [controlTower]
    format: zip
    name_template:  >-
      control_tower_{{ .Version }}_{{ .Os }}_{{ .Arch }}__{{ index .Env.TAG_VERSION 0 }}_{{ index .Env.TAG_VERSION 1 }}
release:
changelog:
  skip: true